#include<stdio.h>
int digisum(int n);
int main()
{
    int T,N,D,j=0;
    scanf("%d",&T);
    while(T--)
    {
        scanf("%d%d",&N,&D);
        while(j<10)
        {
            while(n)
            {

            }
        }


    }
}
